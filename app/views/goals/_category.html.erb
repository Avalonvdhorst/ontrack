<%= f.fields_for :category do |category| %>
  <%= f.input :name, collection: Category.all, include_blank: false %>
  <%= f.input :custom_name, label: false %>
  <%= f.fields_for :goal_category do |category| %>
    <%= category.input :score, as: :integer %>
  <% end %>
<% end %>
